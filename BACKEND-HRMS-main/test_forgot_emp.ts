import { authController } from './src/controllers/auth.controller'; import express from 'express'; const req = { body: { emailOrUsername: 'employee1@vrpigroup.co.in' }, headers: { origin: 'http://localhost:3000' } }; const res = { status: (s: number) => ({ json: (d: any) => console.log('STATUS', s, 'JSON', d) }) }; authController.forgotPassword(req as any, res as any).then(() => console.log('Done')).catch(console.error);
